const assert = require("assert");

describe("Registration", () => {
    it("should allow anonymous user to sign up", () => {
        assert.equal(true, false);
    });
    it("should reject invalid email address", () => {
        assert.equal(true, false);
    });
});
